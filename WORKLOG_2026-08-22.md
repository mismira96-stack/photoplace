# PhotoPlace Worklog - 2026-08-22

## Today

- Preview 완료 dialog의 기본 행동을 `장소별 앨범 만들기`에서 `발견한 장소 보기`로 변경했다.
- `앨범 만들기`는 보조 버튼으로 유지해 기존 Physical organize flow를 보존했다.
- 분석 완료 직후 홈에 다시 나타나던 중복 대형 앨범 만들기 CTA를 숨겼다.
- 결과 화면의 전체 `새 항목만 정리` flow는 유지했다. 오해를 주던 Memory detail의 임시 장소별 정리 버튼은 제거했다.
- `발견한 장소 둘러보기`의 compact list를 photo-first card grid로 변경했다.
- 카드 렌더링은 `MemoryBrowserGridRenderer`로 분리했다. `MainActivity`는 navigation과 thumbnail loading 연결만 담당한다.
- 일반 폭에서는 2열, 600dp 이상 넓은 화면에서는 3열을 사용한다.
- 각 카드는 큰 대표사진 위에 장소명, 사진 수, 날짜 범위를 표시한다.
- 발견 카드를 위치 앨범 카드와 같은 `사진 + 흰 정보 영역` 스타일로 통일했다. 발견 카드는 대표사진 높이만 더 길게 유지한다.
- 발견 카드의 목록 기간은 위치 앨범과 같은 월 단위로 표시하고, 상세에서는 기존 정확한 날짜 범위를 유지한다.
- 하단 navigation을 `홈 | 발견 | 위치 앨범 | 설정` 4탭으로 변경했다.
- 하단 탭 렌더링은 `BottomNavigationRenderer`로 분리했다.
- `정리 기록` 화면명은 실제 Gallery 결과를 뜻하는 `위치 앨범`으로 변경했다.
- 홈 title/CTA를 Memory First 방향으로 변경했다.
  - `PhotoPlace`
  - `사진 속 장소 찾기`
  - `앨범을 만들지 않고 위치별로 먼저 둘러봐요`
- 발견 탭의 데이터는 organized history와 병합하지 않고 DiscoverySnapshot 수와 refs만 표시하도록 수정했다.
- 동일 장소의 Gallery 앨범이 있어도 `앱 안 보기 + 갤러리 앨범` 상태와 Gallery 이동을 발견 UI에 노출하지 않는다.
- 발견 목록과 상세가 동일한 `discovery:` key를 사용하도록 `DiscoverySnapshotController.loadBrowserDetail()`로 조회 경로를 통일했다.
- 발견 상세 동영상 썸네일에 재생 배지를 추가하고 `MemoryPhotoThumbnailRenderer`로 분리했다.
- 발견 탭에 위치 앨범과 같은 아이콘형 검색 진입을 추가했다.
- 검색은 `MemoryBrowserSearch`로 분리하고 장소명, 국가 code/alias, 행정구역, 주소, 연도/월을 지원한다.
- `MemorySearchHeaderRenderer`가 발견 검색 header와 input UI를 담당한다.
- Gallery에서 삭제하거나 휴지통으로 이동한 snapshot URI는 `DiscoverySnapshotLiveFilter`가 MediaStore ID를 묶음 조회해 발견 카드 수, cover, 날짜, 상세에서 제외한다.
- 권한 또는 MediaStore 조회 실패는 삭제로 간주하지 않고 기존 snapshot을 유지한다.
- 발견 탭에 전역 `발견한 장소를 위치 앨범으로 만들기` CTA를 추가했다. 장소별 선택은 제공하지 않고 현재 live discovery 전체를 대상으로 한다.
- `DiscoveryOrganizeConfirmDialog`에서 장소/항목 수, 사진 복사와 원본 유지, 동영상 이동, 기존 항목 건너뜀을 실행 전에 안내한다.
- `DiscoveryAlbumOrganizer`는 snapshot refs를 side-effect 없는 정리 계획으로 변환하고, `DiscoveryAlbumItemAdapter`만 Android `PhotoItem`으로 변환한다.
- `MediaStoreAlbumLookup`은 기존 `장소`/`장소에서` 폴더의 정확한 정규화 일치 경로를 재사용하고 파일명 중복을 확인한다.
- 기존 앨범 확인과 정리 입력 준비는 UI thread 밖에서 실행하며, 실제 파일 작업은 기존 `SortInputStore -> SortWorker -> SortResultStore -> AlbumSummaryHistoryStore` 흐름을 그대로 사용한다.
- 위치 앨범이 없는 화면에는 discovery가 존재할 때 `발견한 장소 보기` 진입 버튼을 제공한다.
- 발견 탭에도 다른 보조 탭과 동일한 `addWorkingBanner()`를 연결해 분석/앨범 생성 중 진행 상태를 표시한다.
- Preview snapshot 생성 시 `duplicateInTarget=true`인 항목은 발견에서 제외한다. 기존 위치 앨범은 존재하지만 해당 파일은 아직 없는 새 항목(`targetExists=true`, `duplicateInTarget=false`)은 발견에 유지한다. 정리 결과 화면의 `이미 정리됨` 표시는 기존대로 유지해 운영 확인 화면과 발견 Inbox의 역할을 분리한다.
- 홈 요약의 상세 action은 분석만 완료된 상태에서 legacy 정리 결과 화면을 열지 않고 `발견`으로 이동한다. 정리 결과 화면은 실제 위치 앨범 생성 완료·중단·원본 정리 workflow에만 유지한다.
- 정리 결과 화면 안에 남아 있던 중복 `발견한 장소 둘러보기` CTA는 제거한다. 발견 진입은 분석 완료 dialog, 홈 CTA, 하단 발견 tab으로 제공한다.
- Preview 완료와 작업 상태 복원 시 legacy 홈 `장소별 앨범 만들기` 버튼이 다시 표시되던 경로를 차단했다. 앨범 생성 진입은 발견 탭의 사용자 확인 전역 CTA로 단일화한다.
- 분석 완료 dialog를 Memory First 기준으로 변경했다. `앨범으로 정리`, `새로 만들 앨범`, `이미 정리됨`, 앨범 후보 미리보기, `앨범 만들기`, `결과 보기`를 제거하고 앱에서 볼 항목/위치 정보 없음과 `발견한 장소 보기`만 제공한다.
- 실기기 crash 확인: 접근 권한이 사라진 MediaStore URI를 외부 viewer로 열 때 `SecurityException`이 발생했다. 사진/동영상 열기를 `openMediaUri()` 안전 경로로 통일해 crash 대신 안내 후 Gallery fallback을 사용한다.
- 실기기 crash 확인: Android 17에서 dataSync foreground service 종료 timeout이 발생했다. 서비스 timeout callback, `onDestroy()` foreground 정리, 직접 `stopService()` 경로를 추가했다.
- 프로세스 crash/업데이트 후 로컬 Preview 분석의 `위치 정보 분석 중` progress가 복원되는 문제를 수정했다. 로컬 분석은 재시작할 수 없으므로 stale 상태를 즉시 종료하고, 실제 앨범 정리 progress만 복원한다.
- Memory First 홈에서 의미가 약해진 `새 장소 / 위치 없음 / 정리 완료` legacy 요약 바를 제거했다. 분석 진행은 진행 UI, 결과는 발견 완료 dialog와 발견 tab에서 확인한다.
- 발견 완료 dialog의 `앱에서 볼 항목`은 동영상 이동 설정과 무관하게 no-location/기존 중복만 제외해 DiscoverySnapshot과 같은 기준으로 집계한다.
- 홈 분석 요약의 legacy `확인 필요 / 정리 예정 / 새 앨범` 문구를 `장소 발견 완료 / 앱에서 볼 항목 / 위치 정보 없음`으로 변경했다.
- 앨범 생성 후 이동된 동영상은 MediaStore ID가 유지되어 발견 탭에 남을 수 있었다. live filter가 `Pictures/*에서/` 위치 앨범 경로를 discovery에서 제외하도록 수정했다.

## Product Direction

- Memory Browser를 `발견` 정식 메인 tab으로 승격했다. Preview 완료 CTA와 하단 tab 양쪽에서 진입할 수 있다.
- 정식 Memory View 출시 전 최소 조건은 lazy date-grouped grid, 대량 사진 탐색, 자연스러운 photo viewer, 검색이다.
- 반복 장소 추천은 보류한다. 장소 -> 날짜 탐색과 검색으로 실제 탐색 요구가 해결되는지 먼저 검증한다.
- 해외 Gallery 정리 단위는 Memory grouping과 분리한다. 국가/여행 세션/도시 단위를 실제 데이터로 비교한 뒤 확정한다.
- lifecycle은 `발견 -> 앱 안에서 보기 -> 위치 앨범 생성 -> 위치 앨범 기록`으로 유지한다.
- 위치 앨범 생성 후 발견 UI에서는 항목을 숨기지만 `discovery_snapshot.json`과 personalization 데이터는 삭제하지 않는다. 향후 발견/위치 앨범이 같은 Memory detail과 메모를 공유할 수 있도록 원본 Memory identity를 보존한다.

## Next

1. Gemini로 전역 CTA, 기존 경로 재사용, WorkManager/history 경계와 실패·중단 처리를 코드 리뷰한다.
2. 일부 위치 앨범을 백업한 테스트 데이터로 기존 경로 재사용과 중복 건너뜀을 한 번 더 회귀 검증한다.
3. 일반폰 2열과 펼친 폴드 3열에서 카드 크롭/텍스트 잘림을 스모크 테스트한다.
4. 대용량 snapshot에서 discovery live-filter, 검색, CTA 준비 단계 성능을 측정한다.
5. Memory detail을 RecyclerView 기반 lazy date grid로 분리 설계한다.
6. 발견/위치 앨범이 공유하는 Memory detail과 날짜별 메모의 stable key를 설계한다.
7. 이후 in-app photo viewer를 구현해 승격된 `발견` tab의 탐색 완성도를 높인다.

## Release Decision

- 이번 MVP에서는 같은 source folder를 다시 분석하면 기존 항목도 다시 읽는다. 기존 snapshot에 중복 누적하지 않고 최신 분석 결과로 갱신하지만, 분석 비용 자체를 피하는 증분 cache는 아직 없다.
- 사용자가 위치 앨범을 생성한 사진은 대상 앨범의 기존 파일 중복 판정으로 다음 discovery snapshot에서 제외한다. 이동된 동영상은 `Pictures/*에서/` live-filter 정책으로 제외한다.
- 위치 앨범을 만들지 않고 앱 안에서만 보는 사용자는 다음 분석에서 같은 장소가 다시 계산될 수 있다. 이는 데이터 손상이나 중복 Memory 생성 문제는 아니며, NoLocationCache v2와 함께 source signature 기반 증분 분석으로 후속 개선한다.
- Gemini가 재제기한 target path, CTA 직전 live filter, 권한 확인, 비동기 prepare 우려는 현재 구현과 실기기 4곳/27개 정리 검증으로 해소된 것으로 판단한다.
- 10k 이상 prepare 성능, 파일명 중복 판정 보강, 중단 후 이어하기는 출시 후 검증 과제로 유지한다.
- `1.2.8 (29)`는 이미 Play에 사용된 버전이므로 이번 Display First / 발견 tab MVP는 `1.3.0 (30)`으로 준비한다.

## Release 1.3.0 Draft

- Feature commit: `9a03dc7 Complete display-first discovery flow`
- Release commit: `8c66468 Prepare 1.3.0 discovery release`
- Version: `versionCode 30` / `versionName 1.3.0` / `targetSdk 36`
- APK: `photoplace-1.3.0-code30-display-first-discovery-api36.apk`
- APK SHA256: `41C2D2939843B8E42882C26DA0A5B2B3212DFA8616816651284F4602B486529A`
- AAB: `photoplace-1.3.0-code30-display-first-discovery-api36.aab`
- AAB SHA256: `D9A2B5BE23FD815CC2FE4F130581D4E7CFFD13CF436A3B621B005017082C9D39`
- Release notes: `release-notes-1.3.0-ko.txt`
- Clean validation: `testDebugUnitTest`, `assembleRelease`, `bundleRelease` 성공.
- Google Play production track에 `1.3.0 (30)` draft 업로드 완료.
- Google API가 `changesNotSentForReview=true`를 지원하지 않아 해당 옵션 없이 edit를 commit했다. 최종 Play Console 검토/제출은 사용자가 수동으로 진행한다.

## Post-Release Local Polish

아래 변경은 `1.3.0 (30)` Play 제출본에 포함되지 않은 다음 버전용 로컬 후속 작업이다.

- 발견 tab의 빈 화면을 텍스트 전용 카드에서 PhotoPlace 시각 empty state로 변경했다.
- 일러스트는 지도, 위치 핀, 사진, 달력을 한 묶음으로 표현하며 AI 기능을 암시하는 문구나 상징을 사용하지 않는다.
- 빈 화면 문구는 GPS 위치 정보 기반 동작을 정확히 설명한다: `사진 속 위치 정보를 확인하면 장소별로 모아볼 수 있어요.`
- `사진 속 장소 찾기` CTA는 홈 hero CTA와 같은 색상/형태 및 PhotoPlace 전용 hero 아이콘을 사용한다.
- 위치 앨범이 0개인 화면에도 같은 계열의 `폴더 + 사진 + 위치 핀` empty-state 일러스트를 추가했다.
- 위치 앨범 empty state는 discovery 데이터가 있을 때만 `발견한 장소 보기` CTA를 노출한다.
- 두 empty state는 각각 `DiscoveryEmptyStateRenderer`, `LocationAlbumEmptyStateRenderer`로 분리했다. `MainActivity`에는 navigation과 기존 button style 연결만 남겼다.
- 일러스트는 실제 사진 카드 화면에는 사용하지 않는다. 발견/위치 앨범 데이터가 존재하면 기존 photo-first UI를 유지하고 하단 tab 아이콘도 변경하지 않는다.

## Gemini Review Triage

현재 로컬 코드와 대조한 결과:

- 해결 완료: 위치 앨범 경로는 `PlaceNamePolicy.safeFolderName()`과 정확 경로 조회를 사용하며 기존 `장소`/`장소에서` 폴더를 재사용한다.
- 해결 완료: CTA 준비와 MediaStore 중복 조회는 `worker` executor에서 수행하며 UI thread를 막지 않는다.
- 해결 완료: 동영상 이동 권한은 기존 `runCopy()` 권한 확인 흐름을 거친 뒤 `SortWorker`를 enqueue한다.
- 해결 완료: CTA 직전에 repository를 다시 로드해 `DiscoverySnapshotLiveFilter`를 적용한다.
- 해결 완료: `duplicateInTarget=true` 항목은 새 discovery snapshot에서 제외하고, 정리 결과 화면에서는 `이미 정리됨`으로 계속 확인할 수 있다.
- 출시 전 실기기 확인: 기존 위치 앨범과 동일 파일이 있는 데이터로 재분석했을 때 발견에서는 빠지고 결과 화면에는 표시되는지 확인한다.
- 출시 전 실기기 확인: 권한 취소와 일부 source URI 삭제 상태에서 CTA가 crash 없이 중단하거나 유효 항목만 처리하는지 확인한다.
- 후속 성능 과제: 10k 이상 discovery refs에서 준비 시간과 메모리를 측정한다. 현재 전역 CTA는 메모리 리스트를 만들지만 UI thread 밖에서 실행하므로 기능 블로커가 아니라 성능 검증 항목으로 둔다.
- 후속 정확도 과제: 파일명 기반 중복 판정에 size/date 보조 신호를 추가할지는 실제 오탐 사례를 수집한 뒤 결정한다. 콘텐츠 hash는 MVP 범위에서 제외한다.

## Validation

```text
.\gradlew.bat testDebugUnitTest assembleDebug
BUILD SUCCESSFUL

adb install -r app\build\outputs\apk\debug\app-debug.apk
Success
```

- Galaxy 기기에서 저장된 discovery snapshot으로 화면 진입 확인.
- 일반 폭 2열 카드, 대표사진, 장소명, 개수, 날짜 범위 렌더링 확인.
- 4개 top-level tab 표시와 선택 상태 확인.
- 발견 tab에서 시스템 Back 시 결과 화면이 아닌 홈으로 복귀하는지 확인.
- 분석 데이터: 2,292개 중 위치 없음 2,263개. 반복 분석 성능은 별도 NoLocationCache v2 과제로 유지.
- 발견 카드가 기존 Gallery 앨범 수를 합산하지 않고 snapshot 수만 표시하는지 확인.
- 기존 Gallery 앨범과 같은 장소인 발견 카드도 상세 진입이 정상 동작하는지 확인.
- 발견 상세에서 Gallery 앨범 버튼이 없고 동영상 썸네일에 재생 배지가 표시되는지 확인.
- Gallery에서 삭제한 2개 항목이 발견 카드에서 즉시 제외되어 `성남 12개 -> 10개`로 갱신되는 것을 확인.
- 발견 검색 icon/input/close 확인, `2026` 검색 결과 4개 확인.
- 장소명, 국가 alias, `8월` 검색은 JVM 단위 테스트로 확인.
- Galaxy 기기에서 discovery 4곳/27개를 전역 CTA로 정리했다.
- 실제 결과는 정리 성공 27개, 건너뜀 0개, 실패 0개, 위치 앨범 4개로 확인했다.
- 사진 복사/원본 유지와 동영상 2개 이동을 확인했다. 동영상 최종 경로는 `Pictures/성남에서/`, `Pictures/수원에서/`였다.
- 정리 완료 후 발견 탭에 이동된 동영상 2개만 남는 회귀를 재현하고, 위치 앨범 경로 제외 패치 후 발견 empty state로 전환되는 것을 확인했다.
- 최신 단위 테스트 107개와 Debug build가 통과했다. 기존 위치 앨범 중복은 discovery에서 제외하고, 기존 앨범의 새 항목은 유지하는 회귀 테스트를 포함한다.
- 20시경 추가 dumpstate 알림을 확인했다. `ApplicationExitInfo`에는 새 ANR이 없었고 최신 종료는 package update/force stop이었다. crash buffer의 별도 `media.swcodec` VP8 encoder tombstone은 Samsung 시스템 미디어 프로세스이며 PhotoPlace 프로세스가 아니다.
- PhotoPlace의 마지막 app crash 기록은 19:57의 stale MediaStore URI `SecurityException`과 Android 17 foreground-service timeout 두 건으로, 위 안전 열기/서비스 종료 패치 이전 기록임을 확인했다. 최신 설치 프로세스는 정상 실행 중이었다.

## Suggested Gemini Review

한국어로 답변해 주세요.

최신 코드에서 Preview 완료 dialog가 Display First 원칙을 지키는지 리뷰해 주세요. `발견한 장소 보기`만 primary action으로 남고 legacy `앨범 만들기`/`결과 보기`가 제거됐는지, 분석 직후 홈에 중복 organize CTA가 노출되지 않는지 확인해 주세요. 앨범 생성은 발견 탭의 사용자 확인 전역 CTA에서만 시작되어야 합니다. 이번 리뷰에서는 추천 알고리즘과 해외 Gallery 앨범 단위는 구현 대상으로 보지 말고 문서상의 후속 POC 경계가 명확한지만 확인해 주세요.

추가로 `MemoryBrowserGridRenderer` 분리가 적절한지, `MainActivity`가 navigation/wiring만 유지하는지, 일반 폭 2열/600dp 이상 3열 계산에서 overflow 또는 빈 공간 문제가 없는지, 긴 한국어 장소명과 날짜 범위가 카드 밖으로 잘릴 위험이 있는지 확인해 주세요. 600dp 기준은 펼친 Galaxy Fold 실기기에서 3열을 검증해 확정했습니다. 현재 상세 화면은 48개 cap을 유지하므로 lazy grid와 in-app viewer는 이번 커밋의 블로커가 아니라 다음 단계 TODO로 구분해 주세요.

최신 navigation 변경도 한국어로 리뷰해 주세요. `BottomNavigationRenderer`가 UI 책임을 분리하는지, `홈 | 발견 | 위치 앨범 | 설정` index가 모든 top-level 화면과 설정 refresh/back stack에서 일관적인지, 발견 목록과 상세의 Back이 올바른지 확인해 주세요. 홈 CTA가 실제 동작인 분석/앱 안 보기를 정확히 설명하고 앨범 생성으로 오해시키지 않는지도 봐 주세요. 발견 카드와 위치 앨범 카드는 같은 시각 체계를 사용하되 발견 대표사진만 더 길게 유지하는 현재 차이가 합리적인지 검토해 주세요.

발견 데이터 경계도 확인해 주세요. `MemoryRepository.discoveryMemories()`가 organized album과 병합하지 않고 DiscoverySnapshot의 count/refs/key만 반환하는지, 목록과 상세가 같은 discovery key를 사용하는지, 발견 UI에서 Gallery 이동 기능이 완전히 빠졌는지 봐 주세요. `memories()`의 MIXED 병합은 향후 통합 대시보드용 내부 API로 남아 있어도 됩니다.

현재 상세의 `이 장소를 앨범으로 정리` 버튼은 선택 장소만 정리하지 않고 전체 결과 화면으로 이동하는 임시 연결이므로 제거할 예정입니다. MVP에서는 장소별 선택을 제공하지 않고, 발견 탭의 전역 CTA에서 현재 발견한 정리 가능 항목 전체를 위치 앨범으로 만듭니다. `앱 안에서만 보기`와 `발견한 장소 전체 앨범 생성` 중 사용자가 선택하는 구조입니다. 다음 패치에 필요한 DiscoverySnapshot/previewItems 데이터 경계, 기존 SortInputStore/SortWorker 재사용, 중복 검사, 앱 재시작 후 previewItems 부재, 실패/중단 및 실제 성공 history 기록 테스트를 제안해 주세요. 반드시 새 Gallery 앨범 생성은 사용자 확인 후에만 수행되어야 합니다.

`MemoryPhotoThumbnailRenderer` 분리와 동영상 재생 배지도 검토해 주세요. 외부 viewer가 한 항목만 여는 현재 한계는 이번 커밋의 블로커가 아니며, 날짜 그룹 단위 앱 내부 스와이프 viewer를 후속 작업으로 분류해 주세요.

후속 로컬 패치에서는 `MemoryBrowserSearch`, `MemorySearchHeaderRenderer`, `DiscoverySnapshotLiveFilter`를 추가했습니다. 검색이 organized history가 아니라 discovery record만 필터링하는지, 국가 alias와 연도/월 검색이 기존 위치 앨범 검색과 일관적인지 확인해 주세요. live filter는 MediaStore ID를 500개 단위로 묶어 조회하고 삭제/휴지통 항목만 read-time projection에서 제외하며 JSON을 즉시 덮어쓰지 않습니다. 권한 오류나 provider 오류를 삭제로 오판하지 않는지, empty group 제거와 count/cover/date 재계산이 맞는지 리뷰해 주세요. 대용량 성능은 후속 실기기 검증으로 분류합니다.

추가 구현된 전역 Discovery CTA를 한국어로 코드 리뷰해 주세요. `DiscoveryAlbumOrganizer`, `DiscoveryAlbumItemAdapter`, `MediaStoreAlbumLookup`, `DiscoveryOrganizeConfirmDialog`의 책임 분리가 적절한지, `MainActivity`에는 확인/비동기 준비/기존 `runCopy()` 연결만 남았는지 확인해 주세요. snapshot ref에서 복원한 정리 입력이 기존 `PhotoItem` 계약과 일치하는지, 기존 `장소`/`장소에서` 경로의 정확 일치 재사용이 유사 폴더 오탐 없이 동작하는지, 사진 복사와 동영상 이동 권한이 Worker enqueue 전에 처리되는지 봐 주세요.

앨범 생성 성공 후 lifecycle도 리뷰해 주세요. Discovery snapshot과 personalization 원본은 삭제하지 않고 read-time projection에서 위치 앨범으로 이동한 항목만 숨깁니다. 특히 동영상은 이동 후 MediaStore ID가 유지되므로 `DiscoverySnapshotLiveFilter`가 `Pictures/*에서/` 경로를 제외하는 현재 정책이 기존 위치 앨범 규칙과 맞는지, 일반 Pictures 폴더를 과도하게 제외하지 않는지 확인해 주세요. partial success/중단/권한 취소/앱 재시작, 10k 이상 입력의 준비 비용은 블로커와 후속 성능 과제로 구분해 주세요. 코드는 수정하지 말고 finding을 심각도순으로 제시해 주세요.

### Final Gemini Review Addendum

한국어로 답변하고 코드는 수정하지 마세요. 최신 커밋 전체를 기준으로 finding을 심각도순으로 제시해 주세요.

1. 분석 완료 dialog와 홈에서 legacy `앨범 만들기`/`결과 보기`/3분할 상태 바가 완전히 제거됐는지, 앨범 생성은 발견 탭의 사용자 확인 전역 CTA에서만 시작되는지 확인해 주세요.
2. `DiscoverySnapshotMapper`가 `duplicateInTarget=true`를 발견에서 제외하면서, 기존 위치 앨범에 없는 새 항목은 유지하는지 확인해 주세요. 결과 화면의 `이미 정리됨` 집계는 기존대로 유지되어야 합니다.
3. `앱에서 볼 항목` 집계가 동영상 이동 설정과 무관하게 DiscoverySnapshot 기준(no-location/중복 제외)과 일치하는지 확인해 주세요.
4. Gallery에서 삭제한 stale URI를 누를 때 발생했던 `SecurityException`을 `openMediaUri()` 공통 경로가 안전하게 처리하는지 확인해 주세요.
5. Android 17에서 발생한 `ForegroundServiceDidNotStopInTimeException` 대응으로 `SortForegroundService.onTimeout()`, `onDestroy()`, 직접 `stopService()`가 적절한지 확인해 주세요. 완료 notification 경로를 깨뜨리지 않는지도 봐 주세요.
6. crash/앱 업데이트 후 로컬 `위치 정보 분석 중` progress를 복원하지 않고 실제 앨범 정리 progress만 복원하는 정책이 현재 실행 구조와 맞는지 확인해 주세요.
7. `DiscoverySnapshotLiveFilter.isOrganizedLocationPath()`가 이동된 동영상을 숨기되 일반 Pictures 폴더를 과도하게 제외할 위험이 있는지 평가해 주세요.
8. 전역 CTA 준비가 UI thread 밖에서 수행되고 기존 `SortInputStore -> SortWorker -> SortResultStore -> AlbumSummaryHistoryStore` 계약을 지키는지 확인해 주세요.

실기기에서는 discovery 4곳/27개 정리 성공, 사진 복사, 동영상 이동, 이동 후 발견 empty state, 삭제 URI crash 재현을 완료했습니다. 10k 이상 성능, 증분 분석 cache, 중단 후 이어하기, lazy detail grid는 이번 출시의 기능 블로커와 후속 과제를 구분해 주세요.

방금 생성된 dumpstate도 판정에 포함해 주세요. 새 PhotoPlace ANR 기록은 없고, 최신 종료는 package update/force stop입니다. `media.swcodec` tombstone은 시스템 미디어 프로세스이며 앱 crash로 분류하지 않습니다. 19:57 PhotoPlace crash 두 건은 이번 패치가 대응한 기존 재현 기록입니다.
