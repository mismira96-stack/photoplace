# PhotoPlace Handoff Update - 2026-07-24 Late

## Current Branch / Safety

- Current local branch: `codex/photoplace-v2-bg-wip`
- Work is local only unless explicitly pushed/merged later.
- Keep using small commits. Current worktree should be clean before resuming.
- Debug APK was installed on the connected phone after uninstalling the existing release app because debug/release signatures differed.

## Latest Background Refactor State

- Activity sort execution now routes through `SortJob`.
- Added reusable background-prep classes:
  - `SortJob`
  - `SortJobResult`
  - `SortJobProgressListener`
  - `MediaCopyEngine`
  - `MediaMetadataReader`
  - `AlbumSummaryHistoryStore`
  - extracted models: `PhotoItem`, `AlbumFolder`, `SourceFolder`, `AlbumSummary`, `StoredAlbumSummary`, `DateRange`, `LocationResult`, `LocationLookupResult`, `ExifReadResult`, `VideoMetadataResult`
- `SortWorker` still exists only as a foreground shell. It does not yet own real scan/copy.
- Release build currently fails lint because WorkManager `SystemForegroundService` needs `foregroundServiceType="dataSync"` manifest override.

## Immediate Test Plan

- User prepared about 4000 photos in a test/download folder.
- Test the installed debug APK:
  - select Download/source folder
  - run scan
  - press `장소별 앨범 만들기`
  - verify copied photos, moved/skipped videos, completion history, recent places, overseas records
  - test leaving app and returning during sort
  - test stop/cancel flow

## Newly Reported Issues

1. Back key can feel unresponsive during work or after repeated navigation.
2. Notification progress does not update live enough.
   - Add notification content intent so tapping notification opens PhotoPlace.
3. Re-running after partial/previous sort is confusing.
   - Duplicate copying appears prevented because rerun shows many `이미 정리됨` items.
   - UX should clearly separate `새로 정리할 항목` from `이미 정리된 항목`.
   - If most items are already sorted, CTA should be hidden or changed to `새 항목만 정리`.
   - This connects to user feedback asking for visible sort history.

# PhotoPlace Handoff - 2026-07-23

## Release Candidate Notes - 2026-07-23 Late Fixes

- Version is now `versionCode 21` / `versionName 1.2.0`.
- `compileSdk` and `targetSdk` are now 36 for the Google Play target API requirement.
- `NoLocationCache.java` exists, but no-location cache is disabled for the 1.2.0 release candidate.
  - `MainActivity.isNoLocationCacheEnabled()` returns `false`.
  - Reason: an early cache pass could make sortable items disappear as `0` candidates.
  - Re-enable only after dedicated invalidation tests.
- Latest debug verification:
  - Sorting candidates returned after disabling the cache.
  - Namhae test sorted successfully.
  - Home recent places now shows the newest saved session immediately.
  - Place detail Back now returns to the previous source screen instead of always Home.
- Before Play upload, rebuild release APK/AAB again. Older release artifacts were created before these late fixes.

## Background Processing Start - 2026-07-24

- Added foreground progress plumbing:
  - `SortForegroundService`
  - `SortProgressStore`
  - `SortNotificationHelper`
- Added WorkManager dependency and initial `SortWorker` foreground shell.
- Current state:
  - Existing Activity-driven sorting now updates a foreground notification and persisted progress.
  - `SortWorker` compiles, but the real sort engine is not moved into it yet.
- Next required extraction:
  - Move scan/copy/history logic out of `MainActivity`.
  - Candidate classes: `SortJob`, `SortJobProgress`, `MediaCopyEngine`, `AlbumSummaryStore`.
  - After extraction, `SortWorker.doWork()` should own scan/copy and the UI should only observe progress.

## Current State

- Project: `PhotoPlace / 앨범정리`
- Path: `C:\Users\mismi\Documents\Codex\GallerySorter`
- Package: `com.photoplace.app`
- Branch: `main`
- Version: `versionCode 21` / `versionName 1.2.0`
- V1 is released.
- AAB/release build should only be generated on explicit request.

## What Changed Today

### V2 1st Slice

Implemented the first V2 Memory View slice:

- Home now shows `해외 기록`.
- Overseas cards are visible on the first screen.
- Cards show thumbnail, country, photo count, and month date range.
- Tapping a country opens an in-app filtered memory view.
- Existing file copy/move behavior is unchanged.

### New Files

- `app/src/main/java/com/example/gallerysorter/MemoryItem.java`
- `app/src/main/java/com/example/gallerysorter/MemoryGroup.java`
- `app/src/main/java/com/example/gallerysorter/OverseasMemoryGrouper.java`
- `app/src/main/java/com/example/gallerysorter/NoLocationCache.java`

### MainActivity Touch Points

- `StoredAlbumSummary` now carries `countryName`, `adminArea`, `addressLine`.
- `album_summary_history.json` writes those fields for new summaries.
- Existing generated albums can be rebuilt with metadata.
- Home uses `loadRecentAlbumSummariesForUi()` to avoid repeated MediaStore scans during quick tab/back navigation.
- No-location scan cache is wired into image/video source loading.

## Existing User Migration

Existing users have old generated folders but may not have country metadata.

Supported paths:

1. Automatic one-time backfill after update if permissions are already granted.
2. Manual Settings action: `발견 장소 다시 만들기`.

Manual rebuild is still important as a recovery option.

## Device Verification

On connected Galaxy:

- `assembleDebug`: passed.
- Debug APK install: passed.
- Home first screen shows overseas records.
- Overseas countries observed: Japan, Australia, Guam.
- Records tab opens.
- System Back returns to Home.

## Important Product Notes

- V2 direction: photo sorting -> memory structure.
- Gallery is fallback, not the primary navigation.
- Filtered Memory View is the app's core navigation idea.
- Do not move to Compose yet.
- Do not keep adding all logic to `MainActivity`; new logic should be separated where practical.

## Next Priority Candidates

### P0. Background Sorting

User feedback: sorting should continue when the app is backgrounded.

Recommended approach:

- Move sorting work out of `MainActivity`.
- Use `ForegroundService` with notification progress.
- Let app UI reattach to active job state.
- Keep original-photo trash action as an app-screen/user-confirmed step.

Avoid squeezing this into the current V2 UI patch.

### P1. Structure Cleanup

Extract from `MainActivity`:

- `AlbumSummaryStore`
- `HomeMemoryRenderer`
- `SortJob`
- `SortProgress`

### P1. Existing User Validation

- Test automatic backfill on a larger library.
- Test Settings -> `발견 장소 다시 만들기`.
- Confirm no false overseas grouping for domestic romanized place names.

### P1. Memory View Polish

- Country filtered view header can become richer.
- Country cards could show a `전체 보기` affordance if many countries exist.
- Consider hiding or compressing recent places further if overseas/travel memories become the main Home story.
