# Worklog 2026-08-24

## Scope

Low-risk V2 polish plus the first Discovery consistency fixes. No media analysis cache is enabled in this patch.

## Completed

- Location Albums header now says `위치 앨범 N개` and photo totals use `사진 N장`.
- Root Settings no longer shows a Back affordance.
- Discovery Memory detail keeps `발견` selected in bottom navigation and the secondary return action says `다른 장소 보기`.
- `DiscoverySnapshotController` now calculates the number of places updated in the current analysis from newly incoming media URIs, not only absent place keys.
- `DiscoverySnapshotLiveFilter` no longer assumes every `Pictures/*에서` folder is managed. It receives actual organized relative paths from loaded album history.
- Discovery cards now preserve each media URI's first-seen snapshot version. Only places with photos newly added in the latest analysis rise to the top with `NEW` and `이번에 +N장`.
- Discovery card totals now use the same `사진 N장` wording as Location Albums.
- Discovery detail's summary card now keeps only photo count and date range; the repeated category label and explanatory copy were removed.
- Discovery tab replaces its explanatory sentence with a compact summary card: place count, photo count, and overall year range.

## Tests

- Added coverage for repeated URI analysis and a new URI in an existing place.
- Added coverage that reanalysis preserves first-seen version while a new URI receives the current version, and that a recently updated discovery card sorts first.
- Added coverage that only paths recorded as organized albums are filtered.
- `./gradlew.bat testDebugUnitTest` passed.
- `./gradlew.bat assembleDebug` passed.
- Gemini review of `f56c38e` found no blocker. It confirmed URI-based first-seen preservation, recent-photo counting, discovery-only NEW boundaries, and repeat-analysis NEW clearing.
- Deferred test hardening: equal `recentAddedCount` tie-break, NEW suppression for organized/mixed records, and URI place reclassification with first-seen preservation.

## Device Note

- A Play-signed installed app cannot be updated with the debug-signed APK. Do not uninstall automatically because that clears app-private discovery snapshot data.
- Device verification needs either a user-approved uninstall/reinstall or a separately prepared release-signed test build.
- After a user-approved uninstall/reinstall, the Band source analyzed to 17 discovered places. A second analysis reported 0 newly updated places and the Discovery History total remained 17. The snapshot no longer loses groups on repeat analysis.

## Next

1. Continue the separate `duplicateInTarget` identity-policy investigation; do not simply include all duplicates. The clean Band baseline still has 17 places rather than the earlier observed 19, so two places are excluded before snapshot persistence.
2. Start `MediaAnalysisStore` as a tested, disabled data-layer step before enabling incremental analysis.
3. Small visual polish: unify Discovery/Location Albums header background surfaces and soften the red date divider in Discovery detail.
