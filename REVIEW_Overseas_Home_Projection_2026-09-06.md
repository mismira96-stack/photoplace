# PhotoPlace 해외 기록 프로젝션 & 홈 연동 최종 코드 리뷰 (2026-09-06)

- **대상 커밋 체인**:
  - `574fe02` 홈 해외 Memory projection 연결
  - `3bff7c6` 홈 projection/live-filter 백그라운드 스레드 이동 (렉 해결)
  - `5877a70` 국가 카드에서 discovery/organized 통합 검색 목록 열기
  - `830edfa` organized Memory의 국가 식별 보강
  - `67f7c6b` 해외 카드 source summary 2줄 표시 (`발견 N곳 \n 위치 앨범 M개`)
- **작성일**: 2026-09-06
- **리뷰 주체**: Gemini (Antigravity) $\rightarrow$ For Codex
- **상태**: **Phase 3-A 기능 구현 승인 (Interim Approved) & UX 차기 방향 확정**

---

## 1. 종합 평가

1. **홈 진입 성능 정상화 (`3bff7c6`)**:
   - `liveFilter`의 대규모 MediaStore 조회를 `worker.execute` 백그라운드 스레드로 격리하여, **UI thread를 대규모 조회가 직접 막는 문제는 해결**되었습니다.
   - 다만 cold start에서는 Java 코드로 생성하는 초기 View 비용과 worker 완료 후 카드가 채워지는 체감 간격이 남을 수 있어, 이를 별도 polish 항목으로 구분합니다.
2. **데이터 누락 방지 (`5877a70`, `67f7c6b`)**:
   - 홈 카드에서 `발견 N곳`과 `위치 앨범 M개`를 2줄로 명확히 분리 표기하여 수량 혼동을 없앴습니다.
   - 클릭 시 `repository.memories()`를 검색하도록 확장하여, 발견 장소와 위치 앨범이 검색 결과에 함께 노출되도록 보완되었습니다.
3. **데이터 안정성**:
   - 갤러리 파일, MediaStore row, 스냅샷, 날짜 메모에 대한 무단 변형 0건 (`No Gallery Mutation`).

---

## 2. 🔍 "해외 발견 연결이 조금 어색한 이유" (UX 원인 분석)

사용자/개발자가 **"연결이 조금 어색하다"**고 느끼는 이유는 버그가 아니라 **화면 성격(Screen Context)의 차이** 때문입니다:

```
[현재 흐름]
홈 '일본' 카드 클릭 ──► '발견 둘러보기' 전체 탭으로 이동 (검색창에 '일본' 자동 입력)
```

1. **탐색 맥락의 분리감**:
   - 사용자는 **"일본 여행 모음집(전용 해외 페이지)"**으로 들어간다고 기대하지만, 실제로는 **"발견 검색창에 '일본'을 대신 쳐준 화면"**이 열립니다.
   - 검색어를 지우거나 뒤로가기를 누르면 '전체 발견 장소'로 빠지므로 홈에서 시작된 '해외 여행' 맥락이 끊깁니다.
2. **과도기적 해결책(Interim)으로서의 역할**:
   - 현재 방식은 **별도 UI를 새로 그리지 않고 기존 검색 기능을 재사용해 발견+앨범을 모두 볼 수 있게 만든 가장 안전한 징검다리(Phase 3-A)**입니다.

### Cold start 체감 지연

최초 앱 실행의 작은 굼뜸은 홈 live-filter 프리징과 별개의 현상일 수 있습니다.

- `buildUi()`가 XML inflate 없이 Java에서 많은 View/Drawable을 동기 생성합니다.
- 홈 기본 골격은 먼저 표시되지만, 해외 기록과 최근 장소는 worker 계산 후 뒤늦게 채워집니다.
- 이 2단계 로딩 간격이 짧더라도 사용자는 “화면이 늦게 완성된다”고 느낄 수 있습니다.

따라서 다음 성능 작업은 무작정 더 비동기화하기보다 `buildUi()` 완료/첫 프레임 시점과 worker projection 완료 시점을 각각 측정한 뒤,
가벼운 이전 홈 projection 캐시 또는 placeholder 적용 여부를 결정합니다.

---

## 3. 💡 어색함을 완전히 없애는 최종 해결책 (Phase 3-B)

기존 위치 앨범 전용 화면이었던 [`showOverseasMemoryScreen`](file:///c:/Users/mismi/Documents/Codex/GallerySorter/app/src/main/java/com/example/gallerysorter/MainActivity.java#L5608)을 **`해외 국가 통합 상세 화면`**으로 발전시키는 것입니다:

```text
┌──────────────────────────────────────────────────────────┐
│  < 뒤로가기                    일본                       │
├──────────────────────────────────────────────────────────┤
│  [ 일본 대표 사진 ]  일본의 기억                         │
│                     사진 48장 · 2026.08.01 ~ 2026.08.05  │
├──────────────────────────────────────────────────────────┤
│  📍 새로 발견한 장소 (2)                                 │
│  ┌────────────────────────────────────────────────────┐  │
│  │ [오타루 썸네일]  오타루 · 사진 12장               │  │
│  │ [신치토세 썸네일] 신치토세 · 사진 8장             │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  📁 정리된 위치 앨범 (1)                                 │
│  ┌────────────────────────────────────────────────────┐  │
│  │ [삿포로 썸네일]  삿포로에서 · 사진 28장           │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

> 이렇게 바뀌면:
> 1. 검색창으로 튕기지 않고 **"일본 전용 여행 페이지"**로 자연스럽게 진입합니다.
> 2. 홈 카드에 표시된 `발견 2곳 / 위치 앨범 1개`가 한 페이지에서 위아래 섹션으로 완벽히 일치합니다.

---

## 4. 최종 판단 (Final Verdict)

> 🟢 **Phase 3-A 마일스톤 승인 (Check-in Ready)**
> 
> - 백그라운드 스레드 분리로 홈 진입 속도가 회복되었고, 발견/앨범 데이터가 정상 표출됩니다.
> - 지금 단계에서 무리하게 대규모 UI 공사를 이어가지 않고, **현재 커밋(`f212e82`)을 안전한 체크포인트로 고정**한 뒤, 다음 세션에서 **`Phase 3-B: 해외 국가 전용 상세 뷰어`**를 진행하시는 것을 강력히 권장합니다! 👏
