# Worklog - 2026-09-05

## Memory Grouping Phase 1

- Completed the single-place Gallery album spike and documented the result in `TODO.md`.
  - Existing Gallery execution can reuse `OrganizePlaceService`, `DiscoveryAlbumOrganizer`, `MediaStoreAlbumLookup`, and `SortWorker` for one place.
  - Actual implementation remains deferred until the Memory lifecycle can retain date notes after organization.
- Added `MEMORY_GROUPING_SPIKE_2026-09-05.md`.
  - Grouping is defined as an app-only collection layer that references original Places. It is not a Gallery merge.
  - The recommended Group detail hierarchy is `date -> place -> note -> photos` so same-day Place notes never flatten into one note.
- Added the isolated Phase 1 data layer:
  - `MemoryCollection`: immutable collection metadata and stable member references.
  - `MemoryCollectionStore`: `memory_collections.json` persistence with tmp/bak atomic writes and corrupt-file safety.
  - MVP policy: one stable Memory ID may belong to one active collection only.
  - Supports create, rename, and dissolve at the store boundary only. No UI invokes these APIs yet.
- Added `MemoryCollectionStoreTest` coverage for create/read, one-Place-one-Group, rename/dissolve, invalid input, backup recovery, and unsafe corrupt-file write refusal.

## Memory Grouping Phase 2

- Added the read-only projection layer before any UI work.
  - `MemoryCollectionResolver` resolves current discovery records through `MemoryIdentityRegistryStore` and never writes to the snapshot, notes, or Gallery.
  - `GroupMemoryDetail` models `date -> place -> note -> photos`; it deliberately does not reuse the flat single-place `MemoryPhotoSection` model.
  - A collection-wide `sourceUri` set removes duplicated media defensively while leaving the original discovery groups untouched.
  - Members that are currently unavailable remain in `memory_collections.json`; the live projection simply omits their missing media.
- Added resolver tests for same-day Place note ownership, newest-first date order, URI deduplication, and unavailable members.
- No selection UI, MainActivity wiring, Gallery album creation, or lifecycle mutation was added.

## Verification

- `testDebugUnitTest` passed.
- `assembleDebug` passed after the Phase 2 projection changes.
- `assembleDebug` passed.

## Scope Kept Out

- No `MemoryIdentityRegistryStore` wiring yet. Group creation UI will resolve selected discovery aliases to stable IDs in the next phase.
- No collection repository, media aggregation, date-group model, viewer, search projection, or selection UI.
- No Gallery album creation, MediaStore mutation, or organization lifecycle changes.

## Next Safe Step

Implement the read-only collection resolver:

```text
selected discovery aliases
  -> stable member IDs
  -> resolve current MemoryRecord values
  -> deduplicate live source URIs
  -> Group date -> Place -> note -> photos model
```

Keep Group UI and Gallery organization out until this resolver has focused unit tests.

## Architecture Update Review

- Confirmed from code that Gallery organize completion currently writes `AlbumSummaryHistoryStore` after `SortResultStore`/worker completion, but does not write a stable Memory organization link or delete the DiscoverySnapshot.
- Confirmed that the current home `해외 기록` path is `StoredAlbumSummary -> OverseasMemoryGrouper`; discovery-only overseas Memories are absent even though they are available to `MemoryRepository.discoveryMemories()` and search.
- Product baseline updated: Memory is canonical; Gallery Album is optional output. `ORGANIZED` should be derived from an active successful organization link rather than stored as a second mutable Memory state.
- Organization link design is generic for future `MEMORY` and `COLLECTION` subjects; album name/path remain last-known Gallery output metadata.
- Recommended order: investigate/implement read-only Overseas Phase 3-A only after choosing its country-level source boundary, then add the organization identity bridge, then unified overseas Memory. Do not union Discovery and Album History by URI alone.

## Overseas Phase 3-A Implementation Spike

- Added `OverseasCountryProjection` as a read-only country container.
  - Discovery-only, organized-only, and both-source countries are represented without mutating either source.
  - A country is emitted once, while discovery `MemoryRecord` values and legacy `StoredAlbumSummary` values remain separate inside the projection.
  - Korean entries are excluded using the existing `CountryIdentityNormalizer` rules.
- Reused the existing `OverseasMemoryGrouper` country detection through a package-level helper; no country heuristic was duplicated.
- Added unit tests for discovery-only, organized-only, both-source, and Korean exclusion cases.
- Deliberately did not connect the projection to `MainActivity` or change the current home UI. Tomorrow's work is the click/detail policy and three-state device check.

## Overseas Phase 3-A Home Projection

- Connected `OverseasCountryProjection` to the home `해외 기록` section.
  - Discovery-only countries now appear even when the user has not created a Gallery album.
  - A country card with discovery Memory opens the discovery list filtered by that country.
  - Organized-only countries continue to use the existing Gallery album detail flow.
- Kept discovery and organized sources separate inside the projection. This is not yet the final stable-ID deduplicated lifecycle view.
- No Gallery files, MediaStore rows, DiscoverySnapshot data, or date notes are mutated.
- Verification: `testDebugUnitTest`, `assembleDebug`, and connected-device launch passed.
- Next review points: both-source duplicate presentation, country-card detail policy, and stable organization-link wiring.

## 2026-09-06 Overseas Home Connection

- Connected the read-only overseas projection to the home `해외 기록` section.
- Discovery-only countries now appear without requiring Gallery album creation.
- Discovery country cards open the discovery list filtered by country; organized-only cards retain the legacy Gallery detail path.
- Reused the already live-filtered album list so the home first frame does not query MediaStore twice.
- Added `REVIEW_Overseas_Home_Projection_2026-09-06.md` for Gemini review.
- Verification passed: `testDebugUnitTest`, `assembleDebug`, clean APK install, and device launch.

## 2026-09-06 Home Projection Performance

- 해외 Memory projection 연결 뒤 홈 진입이 느려지는 현상을 점검했다.
- MediaStore live-filter와 MemoryRepository 생성이 UI 흐름에서 실행되던 경로를 `worker` 백그라운드 계산으로 이동했다.
- 동일 프레임에서는 MemoryRepository를 한 번만 생성하고, 발견 둘러보기 CTA와 해외 기록 projection이 같은 결과를 공유하도록 정리했다.
- UI thread에서는 첫 프레임을 먼저 표시하고 계산 완료 후 섹션만 바인딩한다.
- `testDebugUnitTest`, `assembleDebug`, 연결 단말 설치 및 실행을 확인했다.
- 발견+위치 앨범이 함께 있는 국가에서 현재 발견 목록으로 우선 진입하는 제한은 다음 UX 단계에서 해결한다.
