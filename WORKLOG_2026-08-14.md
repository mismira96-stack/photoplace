# Worklog - 2026-08-14

## Weekend Restart

Restarted V2 work from the last pushed baseline:

- Branch: `codex/photoplace-v2-bg-wip`
- Previous pushed commit: `cce2dc2 Prioritize memory browsing before personal places`
- Product priority: Memory browsing first, Personal Place later.

## Gemini Design Review

Read Gemini's `Memory browsing first` design and saved it as:

- `MEMORY_BROWSING_FIRST_DESIGN.md`

The design includes:

- Memory browsing first principles.
- `DiscoverySnapshotStore` storage contract.
- `DiscoverySnapshotMapper` direction.
- `MemoryRepository` merge role.
- discovery-only detail by `sourceUri`.
- Discovery vs Organizer separation.
- no-location skip placement.
- sequence diagram.
- example JSON.

Decision for the first code patch:

- Use the already-existing `DiscoverySnapshot` / `DiscoverySnapshotJson` schema.
- Do not introduce a second JSON shape from the design example yet.
- Add only the file-backed store and tests.
- Do not touch `MainActivity`.
- Do not implement Mapper, Repository, UI, or Personal Place yet.

## Code Added

Added:

- `DiscoverySnapshotStore`
- `DiscoverySnapshotStoreTest`

Store behavior:

- Reads missing snapshot as an empty `DiscoverySnapshot`.
- Saves and restores the current `DiscoverySnapshotJson` schema.
- Writes through a temp file and keeps a backup during replacement.
- Restores `.bak` if the main snapshot file is corrupt.
- Refuses to overwrite a corrupt snapshot if no valid backup exists.
- Clears target/temp/backup snapshot files.

## Validation

Passed:

```text
./gradlew.bat testDebugUnitTest
```

Next validation before commit/release-level work:

```text
./gradlew.bat assembleDebug
```

## Next Patch

Recommended next small patch:

1. `DiscoverySnapshotMapper` as a pure class.
2. Convert `List<PhotoItem>` to `DiscoveryPhotoRef` and `DiscoveryMemoryGroup`.
3. Add unit tests with fixed PhotoItem fixtures.
4. Still no UI and no `MainActivity` wiring.
