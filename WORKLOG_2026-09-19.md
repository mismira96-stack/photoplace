# PhotoPlace Worklog — 2026-09-19

## Current status

- 아래 resolver 연결 기록 이후 내부 Photo Viewer 구현, Collection 복귀 경로 보완, 실기기 smoke test까지 완료했다.
- `1.3.5 / versionCode 35`는 Play Console production에 제출되었고 현재 검토 대기 상태다.
- 원본 휴지통 이동은 계속 비활성화한다. 다음 기능 우선순위는 Collection Gallery output 검토와 출시 후 lifecycle 보강이다.

## Memory media resolver 연결

- `MemoryRepository`에 stable alias를 통해 usable Memory `OrganizationLink`를 찾는 read-only lookup을 추가했다.
- `MemoryBrowserState.detail()`이 `MemoryMediaResolver`를 사용하도록 연결했다.
- exact Gallery output이 확인되면 Gallery 미디어만 날짜 섹션으로 표시하고, exact link가 없거나 output이 확인된 상태가 아니면 live Discovery refs 정책을 따른다.
- Gallery와 Discovery 미디어를 합쳐 중복 표시하지 않는다.
- 기존 `MemoryPhotoSection`을 그대로 사용하므로 날짜별 메모의 stable Memory + date ownership과 기존 Collection 계층 전제를 유지한다.
- `DiscoverySnapshotController`에서만 `MemoryMediaStoreReader`를 주입해 `MainActivity`에 MediaStore 조회 로직을 추가하지 않았다.
- 끝 슬래시가 있는 MediaStore `RELATIVE_PATH`, `DATE_TAKEN` 누락, 사진·동영상 통합 정렬은 앞선 resolver 보완 커밋과 테스트로 고정되어 있다.

## 출시 경계 (중간 체크포인트 기록)

- 이 시점에는 내부 Photo Viewer와 Gallery-only 재진입 검증이 아직 남아 있어 Play 출시 게이트를 유지했다.
- 이후 같은 작업일에 내부 Photo Viewer 연결과 실기기 검증을 완료했다. Collection Gallery output과 원본 휴지통 이동은 여전히 후속 과제다.
- 개인 사진 라이브러리에 새 앨범을 만들거나 원본을 휴지통으로 보내는 테스트는 진행하지 않았다.

## 검증

- `./gradlew.bat testDebugUnitTest`: PASS
- `./gradlew.bat assembleDebug`: PASS
- `git diff --check`: PASS

## 다음 작업

- Memory detail thumbnail tap을 외부 chooser 대신 내부 same-place + date Photo Viewer로 연결했다.
- 선택 사진부터 시작해 해당 날짜 전체 미디어를 좌우 swipe로 이동하고, 이전/다음 텍스트 컨트롤을 가벼운 보조 수단으로 제공한다. 위치/전체 개수와 Back 후 상세 위치 복귀도 유지한다.
- 기억 모음 상세도 날짜·장소 section을 viewer에 전달해 동일한 내부 viewer를 사용한다.
- 사진은 내부에서 표시하고 동영상은 기존 Gallery 외부 재생 흐름을 유지한다.
- 리뷰 후 보완: 기억 모음 상세에서 viewer로 들어간 경우 Collection 상세로 정확히 복귀하도록 return target을 저장하고, 화면 회전 시 viewer 분기를 Collection 상세보다 먼저 처리한다.
- viewer 이미지 전환 시 cache miss에서도 기존 bitmap을 유지하는 전용 로딩 경로를 추가해 밝은 placeholder 번쩍임을 줄였다. 일반 리스트 썸네일 로딩 정책은 유지한다.

## 실기기 smoke test

- `R5KL503VHQR`에서 일반 Memory 상세와 기억 모음 상세의 viewer 진입을 확인했다.
- 사진 선택, 이전/다음 및 swipe, 전환 flicker 완화, Back 복귀, Collection 복귀를 모두 확인했다.
- 날짜 메모와 기존 상세 흐름은 유지됐다.
- Back 시 상세 화면을 재렌더링하는 순간 아주 짧은 flicker가 남아 있다. 기능 blocker는 아니며, 추후 overlay 복귀 방식으로 polish할 수 있는 P2 항목으로 남긴다.

## Release 1.3.5

- versionName: `1.3.5`
- versionCode: `35`
- AAB: `app/build/outputs/bundle/release/app-release.aab`
- SHA256: `6C524B02576A8ADFA1ED9A1538CBD5A4D32CCDD8F9B29300B8F9A0504A931D35`
- Play Console `production` track에 `PhotoPlace 1.3.5` draft release 등록 완료.
- `changesNotSentForReview=true` commit 요청은 Google API 503으로 실패했지만, draft 상태 일반 commit fallback은 성공했다.
- 사용자가 Play Console에서 최종 `Send for review`를 수동 실행했다. 현재 Google Play 검토 대기 상태다.
- 실기기에서 사진 viewer 진입, swipe, Back, Gallery-only output, 날짜 메모 보존, Collection detail 복귀를 확인했다.
- Gallery-only output과 Discovery-only photo 모두 단일 source 정책으로 표시되며, 원본 휴지통 이동은 테스트하지 않고 계속 차단한다.
