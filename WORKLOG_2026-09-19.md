# PhotoPlace Worklog — 2026-09-19

## Memory media resolver 연결

- `MemoryRepository`에 stable alias를 통해 usable Memory `OrganizationLink`를 찾는 read-only lookup을 추가했다.
- `MemoryBrowserState.detail()`이 `MemoryMediaResolver`를 사용하도록 연결했다.
- exact Gallery output이 확인되면 Gallery 미디어만 날짜 섹션으로 표시하고, exact link가 없거나 output이 확인된 상태가 아니면 live Discovery refs 정책을 따른다.
- Gallery와 Discovery 미디어를 합쳐 중복 표시하지 않는다.
- 기존 `MemoryPhotoSection`을 그대로 사용하므로 날짜별 메모의 stable Memory + date ownership과 기존 Collection 계층 전제를 유지한다.
- `DiscoverySnapshotController`에서만 `MemoryMediaStoreReader`를 주입해 `MainActivity`에 MediaStore 조회 로직을 추가하지 않았다.
- 끝 슬래시가 있는 MediaStore `RELATIVE_PATH`, `DATE_TAKEN` 누락, 사진·동영상 통합 정렬은 앞선 resolver 보완 커밋과 테스트로 고정되어 있다.

## 출시 경계

- 내부 Photo Viewer는 아직 구현하지 않았다. 현재 썸네일 탭은 기존 외부 미디어 열기 흐름이므로 Play 출시 게이트는 유지한다.
- Collection Gallery output, 원본 휴지통 이동, Gallery-only 미디어의 실기기 재진입 검증도 미완료다.
- 개인 사진 라이브러리에 새 앨범을 만들거나 원본을 휴지통으로 보내는 테스트는 진행하지 않았다.

## 검증

- `./gradlew.bat testDebugUnitTest`: PASS
- `./gradlew.bat assembleDebug`: PASS
- `git diff --check`: PASS

## 다음 작업

- Memory detail thumbnail tap을 외부 chooser 대신 내부 same-place + date Photo Viewer로 연결했다.
- 선택 사진부터 시작해 해당 날짜 전체 미디어를 좌우 swipe로 이동하고, 이전/다음 텍스트 컨트롤을 가벼운 보조 수단으로 제공한다. 위치/전체 개수와 Back 후 상세 위치 복귀도 유지한다.
- 기억 모음 상세도 날짜·장소 section을 viewer에 전달해 동일한 내부 viewer를 사용한다.
- 사진은 내부에서 표시하고 동영상은 기존 Gallery 외부 재생 흐름을 유지한다.
- 실기기에서 사진 viewer 진입, swipe, Back, Gallery-only output, 날짜 메모 보존을 확인해야 한다. 현재 `adb devices`에 연결 단말이 없어 테스트 대기 상태다.
- Gallery-only photo/video와 Discovery-only photo를 각각 실기기에서 확인하고, 날짜 메모·Collection detail·Back 복귀를 검증한다.
