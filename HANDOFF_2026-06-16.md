# AlbumPlace V1 Handoff - 2026-06-16

## 오늘 반영 완료

- 원본 삭제 플로우를 `휴지통 이동` 기준으로 정리했다.
- 결과 화면 카운트 불일치 완화:
  - 정리 직후 화면은 `이미 복사본이 있던 항목`이 아니라 `이번 실행에서 실제 정리된 항목` 기준으로 집계한다.
  - `recentlySortedUriKeys`를 추가해 이번 실행에서 정리된 URI를 별도 추적한다.
- FC 원인 확인 및 방어 코드 추가:
  - 원인: 결과 목록 렌더링 중 `loadThumbnailInto()`가 이미 종료된 `thumbnailWorker`에 작업을 넣어 `RejectedExecutionException` 발생.
  - 수정: 썸네일 executor가 종료된 상태면 작업을 넣지 않고, `RejectedExecutionException`을 잡아서 앱이 죽지 않게 처리.
- Play 업로드용 버전코드 갱신:
  - code6: `versionCode 6`, `versionName 1.0.0`
  - code7: `versionCode 7`, `versionName 1.1.0`
  - code8: `versionCode 8`, `versionName 1.1.1`

## 생성된 릴리즈 파일

- `C:\Users\mismi\Documents\Codex\release-apks\photoplace-1.0.0-code6-closed-beta.aab`
- `C:\Users\mismi\Documents\Codex\release-apks\photoplace-1.0.0-code6-closed-beta-release.apk`
- V1.1 GUI 반영 릴리즈:
  - `C:\Users\mismi\Documents\Codex\release-apks\photoplace-1.1.0-code7-release.aab`
  - `C:\Users\mismi\Documents\Codex\release-apks\photoplace-1.1.0-code7-release.apk`
  - `G:\내 드라이브\Codex APK\photoplace-1.1.0-code7-release.aab`
  - `G:\내 드라이브\Codex APK\photoplace-1.1.0-code7-release.apk`
- 기존 사용자 기록 재생성 반영 릴리즈:
  - `C:\Users\mismi\Documents\Codex\release-apks\photoplace-1.1.1-code8-release.aab`
  - `C:\Users\mismi\Documents\Codex\release-apks\photoplace-1.1.1-code8-release.apk`
  - `G:\내 드라이브\Codex APK\photoplace-1.1.1-code8-release.aab`
  - `G:\내 드라이브\Codex APK\photoplace-1.1.1-code8-release.apk`

## 검증

- `.\gradlew.bat :app:assembleDebug` 성공
- `.\gradlew.bat :app:bundleRelease :app:assembleRelease` 성공
- 삭제 API 직접 호출 검색 결과 없음:
  - `createDeleteRequest`
  - `ContentResolver.delete`
  - `getContentResolver().delete`

## FC 분석 메모

덤프 위치:

- 폰: `/sdcard/Download/Quick Share/dumpstate_app_error/`
- PC 복사본: `C:\Users\mismi\Documents\Codex\GallerySorter\debug-dumps\dumpstate_app_error\`

핵심 콜스택:

```text
java.util.concurrent.RejectedExecutionException
Task ... rejected from ThreadPoolExecutor[Terminated]
at com.example.gallerysorter.MainActivity.loadThumbnailInto(MainActivity.java:2878)
at com.example.gallerysorter.MainActivity.addResultRow(MainActivity.java:2761)
at com.example.gallerysorter.MainActivity.renderPreviewResults(MainActivity.java:2655)
```

## V1 출시 전 남은 확인

- 실제 폰에서 code6 설치/업데이트 후:
  - 결과 화면 썸네일 FC 재현 여부 확인
  - 휴지통 이동 버튼 누른 뒤 버튼 상태가 사라지는지 확인
  - 정리 직후 결과 화면 개수가 미리보기/실제 정리 수와 맞는지 확인
- Play Console Closed test에는 code6 AAB 업로드 필요.

## V1.1 후보

- 정리된 기록 JSON 보관 기반 추가 완료:
  - 앱 내부 저장소 `album_summary_history.json`
  - 정리 완료 직후, 이번 실행에서 실제 복사/이동된 항목만 앨범별 요약으로 저장
  - 저장 필드: `albumName`, `relativePath`, `itemCount`, `startDate`, `endDate`, `thumbnailUri`, `createdAt`
  - 최근 세션 최대 20개 보관
- V1.1 GUI 시안 일부 반영:
  - 홈 하단 `최근 발견한 장소` 카드 추가
  - 저장 기록 기반 전체 보기/장소 상세 화면 추가
  - 장소 상세은 대표 사진 1장, 항목 수, 날짜 범위, `갤러리에서 보기`만 제공
  - 정리 완료 화면 제목을 `이번에 발견한 장소` 중심으로 조정
  - 홈 CTA/통계 카드 컬러를 과하지 않게 연한 톤으로 조정
- 기존 사용자용 `최근 발견 장소 재생성` 추가:
  - `Pictures/%에서/` 앨범 구조만 스캔
  - 위치/EXIF/Geocoder 재분석 없음
  - 앨범명, 대표 미디어 URI, 항목 수, 시작/종료 날짜로 `album_summary_history.json` 재구축
- 장소 상세 화면 UX 개선:
  - 상단에 `항목 수 · 연도 범위` 요약 추가
  - 대표 사진 아래 `첫 방문`, `최근 방문`, `활동 기간`, `최근 방문 후` 정보 카드 추가
  - CTA 문구를 `사진 N개 보기`로 변경
  - 앱 내부 사진 브라우저/그리드 없이 갤러리 이동 역할 유지
- 발견 장소 대시보드 UX 추가 조정:
  - 연도 범위 표기를 `2019/2020`에서 `2019~2020`으로 변경
  - 홈 `최근 발견한 장소` 노출을 3개에서 5개로 확대
  - 재생성 버튼명을 `발견 장소 다시 만들기`로 변경
  - 상세 화면은 `첫 기록`, `최근 기록`만 유지하고 통계성 숫자는 제거
  - 목록 메타 정보를 `개수 · 날짜`에서 `날짜 · 개수` 순서로 변경
  - 발견 장소 목록을 최근 기록 연도/날짜 기준으로 정렬
- `기억 지도` 방향 전환 1차 반영:
  - 앱 타이틀 `기억 지도`
  - 부제 `장소로 돌아보는 나의 시간`
  - CTA `새 기억 불러오기`
  - 대시보드 상단 유틸리티 스탯 카드 제거
  - `정리 결과` 섹션을 `발견된 장소`로 변경
  - 최근 장소 섹션을 `기억된 장소`로 변경
  - 장소 목록은 사진 썸네일 대신 지도 핀 아이콘 사용
- 상세 화면 레이아웃 재조정:
  - 큰 대표사진 중심 화면에서 카드형 요약 화면으로 변경
  - 카드 내부 헤더, 첫/최근 기록 날짜 pill, 최대 3장 미리보기, `갤러리에서 전체 보기` CTA 구성
  - 사진 브라우저가 되지 않도록 미리보기는 3장으로 제한
- 위치 정보 없음 항목을 별도 앨범으로 이동하는 옵션
- 분석 결과 저장 후 앱 재실행 시 다시 보여주기
- 대용량 처리 백그라운드 안정화
- 정리된 앨범 탐색/추억 발견형 홈 화면
- 특정 앨범 날짜별/월별 세분화
