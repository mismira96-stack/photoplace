# PhotoPlace Worklog — 2026-09-20

## Collection / Discovery regression investigation

- 1.3.5 이후 성남·수원에서 재현된 현상을 확인했다. 홈에는 장소가 보이지만 발견기록 탭과 기억 모음 상세에서 정리된 Gallery 미디어가 빠지는 상태였다.
- 실기기 read-only 확인 결과, Memory Collection 해제 자체는 `memory_collections.json`에서 정상 반영됐다. 데이터 삭제가 아니라 화면별 source projection 불일치였다.
- 원인은 발견기록 기본 경로가 `discoveryMemories()`만 사용하고, Collection resolver/card가 Discovery refs만 사용한 점이다. Gallery-only output은 일반 Memory detail과 Home에서는 보이지만 Collection projection에서 누락될 수 있었다.

## 최소 hotfix slice

- 발견기록 기본 목록을 `MemoryRepository.memories()` 기준으로 읽도록 변경해 정리된 Gallery-only 장소도 검색/목록에서 접근할 수 있게 했다.
- Collection detail은 각 멤버 Memory에 대해 `MemoryMediaResolver`와 exact usable `OrganizationLink`를 사용하도록 연결했다. Gallery output이 확인되면 Gallery source, 아니면 Discovery source를 사용한다.
- Collection card의 사진/동영상 수와 대표 썸네일도 같은 resolver 결과를 사용하도록 연결했다.
- `lastKnownAlias` fallback을 추가해 stable ID alias가 현재 record key와 직접 매칭되지 않는 경우에도 Collection member를 찾는다.
- 새 Discovery 미디어가 추가된 mixed Memory의 대표 썸네일은 더 최신 날짜의 Discovery cover를 우선하도록 보완했다.
- 홈 최근 장소 가로 목록은 위치 앨범을 먼저 보여주되, 새 Discovery 장소가 앨범에 가려지지 않도록 최대 3개 앨범 + 새 발견 슬롯으로 조정했다.
- 홈 Discovery 카드 정렬은 분석 유입 수가 아니라 실제 미디어 최신일을 1순위로 사용하도록 변경해, 어제 촬영한 하남이 오래된 날짜의 예술의전당보다 먼저 보이게 했다.
- Gallery output을 기준 source로 유지하되, live Discovery refs 중 Gallery 파일명 정규화 signature와 겹치지 않는 새 미디어만 보수적으로 추가 표시하도록 확장했다. 동일 파일명 정책의 한계는 후속 보강 대상으로 남긴다.

## Verification

- `./gradlew.bat testDebugUnitTest`: PASS
- `./gradlew.bat assembleDebug`: PASS
- `git diff --check`: PASS
- debug APK를 `R5KL503VHQR`에 데이터 보존 방식으로 설치 완료했다. Gallery/MediaStore 및 원본 파일은 변경하지 않았다.
- 205개 JVM unit test PASS, debug APK 재설치 완료.

## Release 1.3.6 preparation

- versionName: `1.3.6`
- versionCode: `36`
- signed AAB: `C:\Users\mismi\Documents\Codex\release-apks\photoplace-1.3.6-code36-release.aab`
- SHA256: `B848718BFBF63993840DA237E8BF830F00A0A230F275D3D7C2D6D3EE6B17BDD2`
- Play Console upload and `Send for review` remain manual. No Play submission automation was run.

## Next smoke test

1. 기존 Collection 멤버 성남/수원 상세에서 Gallery-only 사진 표시 확인
2. Collection 해제 후 발견기록 탭과 장소 검색에서 다시 표시되는지 확인
3. 새 Discovery 사진이 있는 동일 장소에서 9월 19일 이후 항목이 표시되고 기존 파일이 중복되지 않는지 확인
