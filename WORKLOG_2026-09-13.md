# Worklog - 2026-09-13

## OrganizationLink Store Slice

- `MemoryOrganizationLinkStore`를 추가했다.
  - schema-versioned `memory_organization_links.json` 저장.
  - `.tmp -> .bak -> replace` 원자 저장과 유효 백업 복구.
  - 복구할 수 없는 손상 상태에서는 빈 데이터로 덮어쓰지 않고 저장을 거부한다.
- 동일 `requestId` 재처리는 중복 저장 없이 멱등 처리하고, 같은 ID에 다른 payload가 오면 거부한다.
- 한 Memory/Collection의 여러 Gallery output 기록을 보존하며 `MISSING`은 이력으로 남기고 usable output으로 취급하지 않는다.
- 리뷰에서 확인된 replay timestamp 비교 문제를 수정했다. 같은 `requestId`의 결과는 `organizedAtMillis` 차이만으로 충돌하지 않는다.
- 파일 교체가 성공한 뒤 백업 정리 실패가 commit을 거짓 `FAILED`로 바꾸지 않게 했다.
- `OrganizationLink`에서 subject ID prefix 뒤의 값 유무를 검사하고, `relativePath`의 구분자와 끝 슬래시를 정규화한다.
- `findLatestUsableLink()`를 추가해 최신 `SUCCESS`/`PARTIAL`을 한 곳에서 조회하며 `MISSING` 이력은 건너뛴다.
- 저장소 테스트를 보강했다: replay timestamp/경로 정규화, 최신 usable 조회, null 및 linkId 충돌 검증 포함.
- 선택적 `OrganizationRequest`(requestId, subject identity, target album metadata)를 추가하고, 입력 저장소 -> Worker -> 결과 저장소까지 전달할 수 있게 했다. 기존 일반 정리 요청은 필드를 생략해 기존 동작을 유지한다.
- confirmed sort result만 OrganizationLink로 바꾸는 `OrganizationLinkFromSortResult`를 분리했다. 성공한 항목 수/target path가 요청과 일치할 때만 link를 만들고, 실패/취소는 `PARTIAL`, 성공 복사 0건은 link 없음으로 처리한다.
- Background result 소비 전 앨범 이력(requestId idempotency), `path:` alias, OrganizationLink를 기록한다. 각 단계가 재실행돼도 중복 세션/link가 생기지 않게 했고, persistence 실패 시 결과 JSON은 지우지 않고 재시도한다.
- 실제 기존 UI는 OrganizationRequest 없이 동작한다. single-place CTA는 아직 연결하지 않아 현재 갤러리 작업 동작은 그대로다.
- Worker, Gallery/MediaStore, UI/CTA와 사용자 사진은 이번 범위에서 변경하지 않았다.

## Verification

- `testDebugUnitTest`: 통과.
- `assembleDebug`: 통과.
- `git diff --check`: 통과.
- 실기기 설치/검증 및 Play 출시 작업은 수행하지 않았다.

## Next Work

1. `MemoryRepository`가 exact OrganizationLink로 discovered Memory와 Gallery output을 resolve하도록 연결한다.
2. 공용 media resolver를 연결하고, legacy 앨범 이력은 exact identity backfill 정책이 결정되기 전까지 이름만으로 자동 연결하지 않는다.
3. 그 뒤 single-place CTA와 취소/partial/failure/replay 기기 테스트를 진행한다.

## Checkpoint

OrganizationLink 저장소, persisted request/result 전달, idempotent 완료 처리와 성공 결과 link/history/alias 기록까지 완료했다. 단일 장소 CTA 호출부와 Repository/media resolver 연결, 실기기 lifecycle 검증은 아직 남았다.
