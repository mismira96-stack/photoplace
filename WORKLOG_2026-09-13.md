# Worklog - 2026-09-13

## OrganizationLink Store Slice

- `MemoryOrganizationLinkStore`를 추가했다.
  - schema-versioned `memory_organization_links.json` 저장.
  - `.tmp -> .bak -> replace` 원자 저장과 유효 백업 복구.
  - 복구할 수 없는 손상 상태에서는 빈 데이터로 덮어쓰지 않고 저장을 거부한다.
- 동일 `requestId` 재처리는 중복 저장 없이 멱등 처리하고, 같은 ID에 다른 payload가 오면 거부한다.
- 한 Memory/Collection의 여러 Gallery output 기록을 보존하며 `MISSING`은 이력으로 남기고 usable output으로 취급하지 않는다.
- `MemoryOrganizationLinkStoreTest` 5개를 추가했다: MEMORY/COLLECTION 저장, replay/conflict, MISSING, 다중 output history, backup 복구/fail-safe.
- Worker, Gallery/MediaStore, UI/CTA와 사용자 사진은 이번 범위에서 변경하지 않았다.

## Verification

- `testDebugUnitTest`: 통과.
- `assembleDebug`: 통과.
- `git diff --check`: 통과.
- 실기기 설치/검증 및 Play 출시 작업은 수행하지 않았다.

## Next Work

1. Sort Worker의 persisted request/result에 `requestId`와 subject identity를 연결한다.
2. confirmed result에서 실제 성공 항목만 AlbumSummaryHistory, `path:` alias, OrganizationLink에 반영하고 재처리 멱등 통합 테스트를 추가한다.
3. 기존 앨범 이력은 exact identity가 확인되는 backfill 정책이 결정되기 전까지 이름만으로 자동 연결하지 않는다.
4. 그 뒤 MemoryRepository/media resolver 연결, single-place CTA, 실기기 lifecycle 검증 순서로 진행한다.

## Checkpoint

OrganizationLink 모델·저장소·store 단위 테스트까지 완료했다. 다음은 Worker lifecycle 연결이며, 아직 앨범 생성 UI나 실제 Gallery 변경은 연결되지 않았다.
